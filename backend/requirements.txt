# Server runtime (PRD 2: Python 3.11+, FastAPI + Uvicorn, Pydantic v2)
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.11

# Tests (FastAPI TestClient requires httpx)
pytest>=8
httpx>=0.27

# Offline pipeline scripts only. The running server never imports these and
# never reads .env or API keys. python-dotenv is listed by PRD 2 but is never
# imported (standing no-dotenv rule; Google CLIs read the process environment).
python-dotenv>=1.0
PyYAML>=6.0
